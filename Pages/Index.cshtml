@page
@model IndexModel
@using ASP_RazorWeb.Models
@{
    ViewData["Title"] = "Home page";
    var blogs = ViewData["blogs"] as List<Article>;
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
<ul>
@foreach(var blog in blogs) {
    <li>@blog.Title</li>
}

</ul>

@* 
    CRUD
    dotnet aspnet-codegenerator razorpage -m ASP_RazorWeb.Models.Article -dc ASP_RazorWeb.Models.BlogContext -outDir Pages/Blog -udl --referenceScriptLibraries

    Nếu chạy không thành công chạy lệnh dotnet add package Microsoft.EntityFrameworkCore.Tools
 *@